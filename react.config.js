/* @flow */
const defaultConfig = require('react-native/default.config');

module.exports = {
  project: {
    ios: {},
    android: {},
    windows: {},
  },
  dependency: {
    platforms: {
      ios: null,
      android: null,
      windows: {},
    },
  },
};
